# HTTP Squid Proxy

This module contains the infrastructure for a simple HTTP proxy using the https://www.squid-cache.org/ proxy server.

Persistent docbox servers use this proxy to perform scraping activities on the public internet while
maintaining its isolation within a private subnet.
